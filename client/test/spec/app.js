/*global describe, it, chai, client */
'use strict';

chai.should();

(function () {
	describe('App router', function () {
		it('loads the most recent available data by default', function () {
			var appModel = new client.Models.ApplicationModel();
		
		});

		it('checks if the date data is invalid, falling back to most current available', function() {

		});

		it('loads data from the info specified in the URL if present', function() {

		});

		
	});
})();
